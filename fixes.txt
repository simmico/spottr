Create a new website called Spottr by transforming the cloned Dealr codebase. Read every existing file before making any changes so you understand the full structure, naming conventions, component patterns, scroll animations, nav behaviour, mobile nav, and data patterns. Then make the following changes throughout the entire codebase.

SITE IDENTITY: Replace all instances of Dealr with Spottr. Replace all instances of the Dealr tagline with: Less scrolling, more doing. On the about page and anywhere Simmico projects are referenced, add the line: Save with Dealr. Explore with Spottr. with a link to dealr.simmico.co.za. The site is about discovering things to do in South African cities, not deals or rewards or banking.

COLOUR SCHEME: Replace every colour variable and every hardcoded colour throughout all CSS files with the following new design system. Remove all Dealr colours completely. The new colours are: --color-dark: #0D1B2A, --color-mid: #1E3A5F, --color-light: #3A6EA8, --color-coral: #E8603A, --color-coral-light: #F08060, --color-sand: #F5EDD8, --color-sand-dark: #EAE0C8, --color-text-dark: #0D1520, --color-text-mid: #1E2D3D, --color-text-light: #F5EDD8, --color-text-muted: #7A9DB8. Anywhere Dealr used --color-gold use --color-coral. Anywhere Dealr used --color-beige use --color-sand. Anywhere Dealr used --color-mint or --color-light use --color-light. The dark hero to light content scroll transition must be preserved but now goes from midnight blue to warm sand. All gold accents become coral.

PAGES TO KEEP AND TRANSFORM: Keep index.html, transform it into the Spottr homepage. Keep about.html, transform it into the Spottr about page. Delete all other existing HTML pages that are specific to Dealr content like banking pages, rewards pages, discovery pages, card privilege pages, birthday pages. Replace them with the following five pages only: index.html, explore.html, regions.html, escape.html, about.html.

DATA FILE: Create a single file called activities.js in a data folder. This is the single source of truth for all activity content across the entire site. Every page that displays activities reads from this file only. Seed it with 20 dummy activities spread across JHB, CPT, DBN, and PTA. Each activity object must have the following fields: id, name, description, region (one of: jhb, cpt, dbn, pta), category (one of: outdoor, food, arts, family, hidden-gem), vibe tags as an array, duration, cost (one of: free, budget, splurge), address, url. Make the dummy data realistic and South African, use real place names and plausible activity names even if fictional. Spread activities evenly across regions and categories.

INDEX PAGE: Transform the homepage to introduce Spottr. Hero section with the Spottr name, tagline Less scrolling, more doing, and a short subheading about discovering things to do across South African cities. Include a prominent Escape Button in the hero or just below it as a teaser with the label Hit Escape and a keyboard Esc key visual. Include a featured activities section showing 6 random activities from activities.js. Include a how it works section explaining Browse, Filter, Escape. Keep all scroll animations from Dealr.

EXPLORE PAGE: Create explore.html as a full browsable activity feed. At the top, filter chips for region (All, JHB, CPT, DBN, PTA) and category (All, Outdoor, Food, Arts, Family, Hidden Gem). Below the filters, a card grid showing all activities from activities.js, filtered live by the selected chips using vanilla JS. Each card shows the activity name, region, category badge, cost badge, short description, and a link. No page reload on filter, all filtering happens in JS in the browser. Read data from activities.js only.

REGIONS PAGE: Create regions.html as a hub page. Show four city cards for JHB, CPT, DBN, PTA each with a name, short description of what that city offers, and a View Activities button. When a user clicks View Activities for a city, it scrolls down to or navigates to a section on the same page showing all activities for that city filtered from activities.js. Each city section has its own anchor. This is one page, not four separate pages.

ESCAPE PAGE: Create escape.html, this is the hero feature of Spottr. The page concept is The Escape Button, the antidote to doomscrolling, a way to touch grass and get off your screen. The page hero copy must be: You have been staring at this screen long enough. Hit Escape. Below the hero, two filter dropdowns: one for region (All, JHB, CPT, DBN, PTA) and one for category (All, Outdoor, Food, Arts, Family, Hidden Gem). Both default to All. Below the filters, a large keyboard Escape key styled as a real keycap using CSS, pressable, centred on the page. This is the main CTA. When the user presses the Escape key button, the following animation sequence plays using vanilla JS and CSS animations only, no libraries: first a shuffle phase where activity cards blur and cycle rapidly through the filtered list for about two seconds, then a slowdown phase where the cycling slows over one second, then a land phase where it stops on one randomly selected activity from the filtered results, then a reveal phase where confetti bursts using pure CSS and JS, no canvas libraries, and the chosen activity card animates in with a smooth entrance. Below the revealed card, two buttons: one labelled Still glued to your screen? Hit Escape again which resets and reruns the animation, and one labelled Explore everything which links to explore.html. If no activities match the selected filters, show a friendly message instead of running the animation.

ABOUT PAGE: Transform the existing Dealr about page content into the Spottr about page. Read the existing about page content carefully and adapt the tone and copy for Spottr. Include: what Spottr is and why it exists, that it is free with no ads, who built it (read from existing Dealr about content and adapt), the Simmico projects line Save with Dealr. Explore with Spottr. with a link to dealr.simmico.co.za, and how to submit an activity. The submit section must display an email address for submissions, same pattern as Dealr currently does for deal submissions, no form, just the email displayed so users can click it and send an email directly.

NAVIGATION: Read the existing Dealr nav carefully. Replace all Dealr-specific nav links with: Home (index.html), Explore (explore.html), Escape (escape.html), Regions (regions.html), About (about.html). Update the logo and site name to Spottr throughout. Keep the exact same nav behaviour, scroll effects, and desktop nav structure from Dealr but with Spottr content and colours.

MOBILE BOTTOM NAV: Read how Dealr implements its mobile bottom nav. Keep the exact same implementation pattern but replace all items with the following five items in this order: Home with a house icon, Explore with a grid icon, Escape as the centre elevated button with a keyboard Esc key icon rendered in CSS or SVG not an image, Regions with a map pin icon, About with an info circle icon. The Escape button must be elevated above the nav bar and use --color-coral as its background, same treatment as Dealr's centre action button. Use SVG icons inline for all five items.

FOOTER: Update the footer to say Spottr. Keep the same footer structure and scroll behaviour from Dealr. Update all links to match the new five pages. Include the Simmico projects line with the Dealr link.

CSS AND JS: Do not change the animation system, scroll behaviour, intersection observers, or any motion code. Only change colours, content, and structure. Keep every CSS class name that is reused across components so shared styles still apply. If a CSS class is only used by a Dealr-specific page that is being deleted, remove it. Keep everything else.

FONTS: Keep Cormorant Garamond 600 for headings and DM Sans 400 and 500 for body and UI exactly as Dealr uses them.

After completing all changes, confirm which files were created, which were modified, and which were deleted.